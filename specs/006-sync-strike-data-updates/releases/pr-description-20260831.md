## Summary
Automates synchronization of recent strike documentation and Telegram archive filings (SIMA 27/08 mediation minutes, 11-point Strike Committee platform, and Getafe assembly resolutions) with canonical project datasets. Introduces an interactive human-in-the-loop validation gate (`--interactive-review`) before committing dataset changes, high-contrast Amber badges (`[WARN] Información Sensible en Revisión / Negociación Activa`) for provisional figures, and full multi-surface parity across JSON, JavaScript, and Markdown dossiers.

## Specification & Requirements
- **Interactive User Validation Gate (FR-002, FR-003)**: Automatically formats extracted additions and deletions into a markdown review table requiring explicit confirmation before modifying canonical files.
- **Sensitive / Unreviewed Information Badges (FR-004)**: Visual Amber markers on provisional or unratified negotiation cards in the dashboard with contextual tooltips.
- **Multi-Surface Parity & Invariants Gate (FR-005, FR-006)**: Guarantees zero numerical drift across `conflict_metrics.json`, `dashboard/data.js`, and `docs/Guia_Estrategica_Negociacion_Huelga_Airbus_2026.md` while maintaining 100% compliance with Rules 1–14.

## Implementation Highlights
- `src/parsers/telegram_parser.py`: Added proposal extraction for SIMA 27/08 and 11-point platform, plus `generate_strike_update_manifest()`.
- `src/data_ingestion.py`: Added `--interactive-review` and `--apply-all` CLI options, `format_validation_manifest_table()`, and `apply_validated_manifest()`.
- `src/atomic_writer.py`: Added context manager support (`__enter__` / `__exit__`) and `write_json` alias to `AtomicTransaction`.
- `src/parsers/config_loader.py`: Polymorphic parameter support in `get_source_by_id` and interval overrides.
- `dashboard/index.html` & `dashboard/app.js`: Added `.badge-sensitive` styling tokens, `renderSensitiveBadge()` helper, and dynamic rendering of the 11-Point Strike Committee Platform.

## Verification & Testing
- `validate_sources.py`: 100% PASS on HTML tag balance, 58 primary source links, 5 tabs, and 12 canvases.
- `validate_invariants.py`: Rules 1–14 100% PASS with full mathematical, electoral, and financial consistency.
- Unit Test Suite (`python3 -m unittest discover tests`): 31 / 31 tests passing across `test_data_ingestion.py`, `test_dashboard_ui.py`, and `test_analysis_engine.py`.

## Checklist
- [x] All 17 tasks completed and verified
- [x] Pre-flight readiness checks passed (Clean working tree, 100% test pass)
- [x] Invariant validation gate (Rules 1-14) 100% passed
- [x] HTML tag balancing & DOM hierarchy verified
- [x] Changelog updated (`CHANGELOG.md`)
- [x] Release artifacts archived

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
