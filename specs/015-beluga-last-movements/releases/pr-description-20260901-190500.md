## Summary
Release addressing UI and reactivity hardening:
1. Resolved portal tab finance content flash by setting `hidden` on `#tab-overview` in static HTML.
2. Restored dynamic salary proposal chart and KPI reactivity across `salaryEvolutionChart`, `wagesChart`, and `#kpi-diff-custom-5yr` by removing legacy undefined variable references.
3. Expanded regression test coverage to 80/80 passing tests with full invariant compliance.

## Key Changes
- **`dashboard/index.html`**: Default static tab visibility strictly enforced with `hidden` on `#tab-overview`.
- **`dashboard/app.js`**: Purged dead `medBaseSalary` references in `updateWageSimulation()`, unblocking multi-proposal and chart update pipelines.
- **`tests/test_dashboard_ui.py` & `tests/test_chart_resilience.py`**: Added regression tests for static tab visibility and custom proposal chart reactivity.

## Verification & QA
- 80/80 unit and integration tests passing.
- 14/14 mathematical, electoral, and financial invariants verified.
- 100% DOM structure, tag balancing, and primary sources validated.
- 0 syntax or linting errors.

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
