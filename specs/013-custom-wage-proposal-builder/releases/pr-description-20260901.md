## Summary
Eliminates the unverified 9.5% SIMA wage offer from the simulator and replaces it with a dynamic, in-card **Custom Salary Proposal Builder & Simulator** (`#sc3-custom`). Workers, delegates, and analysts can now model custom wage demands with dual keyboard-writable numeric inputs paired with range sliders, configurable IPC linkage, custom RSG margins, hyperinflation caps, transparent mathematical formula tooltips, and full reactivity across Year 1 tables ($S_1$), net monthly pay, and 5-year inflation-adjusted real trajectories ($P_5$).

## Specification
- **User Story 1 (P1 - MVP)**: Clean removal of fictional SIMA wage data; standardization of canonical benchmarks (*Oferta Patronal Airbus +5%* and *Plataforma del Comité +12%*); transformation of Card 3 into *Tu Propuesta Personalizada* with algebraic math tooltips.
- **User Story 2 (P2)**: In-card interactive controls inside Card 3 (paired slider & numeric input 0–50%, keyboard arrears input 0–50.000 € with quick chips, RSG modes, IPC toggle, margin, inflation caps) and 3 instant one-click presets (*Pérdida Cero*, *Recuperación 2030*, *Equilibrio*).
- **User Story 3 (P3)**: Dynamic differential KPI card (*Tu Propuesta vs. Empresa +5%*) and live reactivity in multiannual Chart.js datasets (`#salaryEvolutionChart` and `#wagesChart`).

## Implementation
- **`src/analysis_engine.py`**: Added `evaluate_annual_raise()` with RSG margin and cap modeling, `solve_recovery_initial_raise()` horizon solver ($r^*$), and updated `get_salary_proposals_comparison()` to output canonical structured proposals (Company, CGT, Committee, Custom).
- **`dashboard/app.js`**: Added client-side compounding engine, preset handlers (`setCustomProposalPreset`), bidirectional keyboard/slider synchronization, reactive input listeners, and synced multiannual datasets.
- **`dashboard/index.html`**: Added paired number inputs, quick chips, IPC toggle, cap controls, and pure CSS mathematical formula tooltips (`.math-tip`) detailing April Effect losses, compounding formulas, and tax rates.
- **`CHANGELOG.md`**: Recorded feature release and bug fix details.

## Testing & Quality
- **Unit & UI Tests**: 56/56 passing tests in `tests/test_analysis_engine.py` and `tests/test_dashboard_ui.py`.
- **Invariants Validation**: 100% passed across all 14 mathematical, census, electoral, financial, and source verification rules (`src/validate_invariants.py`).
- **CI Pipeline**: 4/4 green matrix jobs in GitHub Actions (Python 3.10, 3.11, 3.12, and PDF compilation).

## Checklist
- [x] All 20 tasks completed in `tasks.md`
- [x] SIMA unverified figures eliminated from codebase and UI
- [x] Custom proposal builder & presets operational with $<20\text{ ms}$ recalculation
- [x] Keyboard numeric input and slider synchronization validated
- [x] Math formula tooltips transparent across all scenario cards
- [x] CI pipeline green across all Python matrix environments
- [x] Changelog updated in `CHANGELOG.md`
- [x] Mathematical invariants verified 100%

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
