## Summary
Eliminates the unverified 9.5% SIMA wage offer from the simulator and replaces it with a dynamic, in-card **Custom Salary Proposal Builder & Simulator** (`#sc3-custom`). Workers, delegates, and analysts can now model custom wage demands with full reactivity across Year 1 tables ($S_1$), net monthly pay, and 5-year inflation-adjusted real trajectories ($P_5$).

## Specification
- **User Story 1 (P1 - MVP)**: Clean removal of fictional SIMA wage data; standardization of canonical benchmarks (*Oferta Patronal Airbus +5%* and *Plataforma del Comité +12%*); transformation of Card 3 into *Tu Propuesta Personalizada*.
- **User Story 2 (P2)**: In-card interactive controls inside Card 3 (Initial raise slider 0–25%, arrears selector 0–15.000 €, RSG modes, inflation caps) and 3 instant one-click presets (*Pérdida Cero*, *Recuperación 2030*, *Equilibrio*).
- **User Story 3 (P3)**: Dynamic differential KPI card (*Tu Propuesta vs. Empresa +5%*) and live reactivity in multiannual Chart.js datasets (`#salaryEvolutionChart` and `#wagesChart`).

## Implementation
- **`src/analysis_engine.py`**: Added `evaluate_annual_raise()` with RSG margin and cap modeling, `solve_recovery_initial_raise()` horizon solver ($r^*$), and updated `get_salary_proposals_comparison()` to output 4 structured proposals (Company, CGT, Committee, Custom).
- **`dashboard/app.js`**: Added client-side compounding engine, preset handlers (`setCustomProposalPreset`), reactive input listeners, and synced multiannual datasets.
- **`dashboard/index.html`**: AMOLED crystal styling for in-card compact controls and interactive pill buttons.
- **`CHANGELOG.md`**: Recorded feature release details.

## Testing & Quality
- **Unit & UI Tests**: 56/56 passing tests in `tests/test_analysis_engine.py` and `tests/test_dashboard_ui.py`.
- **Invariants Validation**: 100% passed across all 14 mathematical, census, electoral, financial, and source verification rules (`src/validate_invariants.py`).
- **CI Pipeline**: 4/4 green matrix jobs in GitHub Actions (Python 3.10, 3.11, 3.12, and PDF compilation).

## Checklist
- [x] All 20 tasks completed in `tasks.md`
- [x] SIMA unverified figures eliminated from codebase and UI
- [x] Custom proposal builder & presets operational with $<20\text{ ms}$ recalculation
- [x] CI pipeline green across all Python matrix environments
- [x] Changelog updated in `CHANGELOG.md`
- [x] Mathematical invariants verified 100%

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
